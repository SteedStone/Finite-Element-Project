# How to Launch the Program

Follow these steps to launch the program:

1. **Ensure Dependencies are Installed**  
    Make sure you have all the required dependencies installed. If the program uses a package manager (e.g., `pip` for Python), you can install the dependencies using:
    ```
    pip install -r requirements.txt
    ```

2. **Navigate to the Project Directory**  
    Open a terminal and navigate to the project directory:
    ```
    cd /home/Guerand/Documents/Q6/Element finis/Project/LEPL1110---Project
    ```

3. **Run the Program**  
    Execute the program using the appropriate command. For example:
    - If it's a Python script:
      ```
      python main.py
      ```
    - If it's a compiled program:
      ```
      ./program_name
      ```

4. **Provide Necessary Inputs**  
    If the program requires input files or arguments, ensure they are provided. For example:
    ```
    python main.py input_file.txt
    ```

5. **Check Output**  
    The program should now be running. Check the terminal or output files for results.

6. **Troubleshooting**  
    If you encounter any issues, refer to the error messages or consult the documentation for further assistance.

Enjoy using the program!